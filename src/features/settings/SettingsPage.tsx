export function SettingsPage() {
  return (
    <div className="text-muted-foreground p-8 text-sm">
      Settings — theme, defaults, and backup/restore are coming in a later milestone.
    </div>
  )
}
